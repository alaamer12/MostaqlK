<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ds="clr-namespace:MostaqlK.UI.DesignSystem">

    <!--
        Shared base style for AppButton (UI/PlatformComponents/AppButton), following Mechanism 1
        in cross-platform-ui-conventions.md: same shape everywhere, per-OS tweaks via
        Platforms/Windows/Styles/AppButtonStyle.Windows.xaml using Style.BasedOn.
    -->
    <Style x:Key="AppButtonBase" TargetType="Button">
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource AppBackgroundDark}}" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource AccentPrimary}, Dark={StaticResource AccentPrimaryDark}}" />
        <!-- The mockups' buttons are `font-medium` in Tajawal (design/mvp/*.html). -->
        <Setter Property="FontFamily" Value="TajawalMedium" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="MinimumHeightRequest" Value="44" />
        <Setter Property="MinimumWidthRequest" Value="44" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray600}}" />
                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
        <Style.Behaviors>
            <ds:PressableEffect ApplyHoverHighlight="False" />
        </Style.Behaviors>
    </Style>

    <!--
        Compact "outline chip" secondary-action button used where a full AppButtonBase would be
        too heavy (e.g. ProjectCard's "عرض في مستقل" link-out button): a Border (not a Button) so
        it can carry an icon + label pair, styled as a light accent-tinted pill with a hairline
        border. Still gets the same tactile PressableEffect feedback (hover highlight + press
        scale/opacity) as every other clickable unit in the app.
    -->
    <Style x:Key="OutlineChipButtonStyle" TargetType="Border">
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Stroke" Value="{AppThemeBinding Light=#DBEAFE, Dark=#1E3A5F}" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EFF6FF, Dark=#132237}" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="8" />
            </Setter.Value>
        </Setter>
        <Style.Behaviors>
            <ds:PressableEffect ApplyHoverHighlight="True" />
        </Style.Behaviors>
    </Style>

</ResourceDictionary>
