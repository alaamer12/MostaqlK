namespace MostaqlK.UI.PlatformComponents;

/// <summary>
/// Windows-only tweaks for <see cref="AppEntry"/> (V1 scope).
/// </summary>
public partial class AppEntry
{
    // TODO: customize the WinUI TextBox handler mapping here (e.g. caret color, selection
    // highlight) if the default Entry handler needs adjustments beyond what styles can express.

    // A future .Android.cs / .iOS.cs / .MacCatalyst.cs partial would live alongside this file
    // once V3 mobile work actually starts (per the "avoid empty/premature folders" rule).
}
