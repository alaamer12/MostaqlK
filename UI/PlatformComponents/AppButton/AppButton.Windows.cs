namespace MostaqlK.UI.PlatformComponents;

/// <summary>
/// Windows-only tweaks for <see cref="AppButton"/> (V1 scope).
/// </summary>
public partial class AppButton
{
    // TODO: customize the WinUI handler mapping here if the default Button handler needs
    // corner-radius / hover-state / focus-visual adjustments beyond what styles can express.

    // A future .Android.cs / .iOS.cs / .MacCatalyst.cs partial would live alongside this file
    // once V3 mobile work actually starts (per the "avoid empty/premature folders" rule).
}
