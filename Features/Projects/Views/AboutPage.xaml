<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MostaqlK.Features.Projects.Views.AboutPage"
             Title="حول التطبيق"
             FlowDirection="RightToLeft">

    <!-- Minimal placeholder reflecting about.html: title, version, and placeholder body text.
         Placed under Features/Projects/Views rather than a new Features/About folder since it
         is a single static page reachable only from the Projects sidebar nav, not a standalone
         feature slice of its own. -->
    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">
            <Label Text="MostaqlK" FontSize="24" FontAttributes="Bold" />
            <Label Text="الإصدار 1.0" FontSize="14" TextColor="Gray" />
            <Label Text="تطبيق سطح مكتب يرصد مشاريع مستقل الجديدة ويرسل إشعارات فورية عند ظهورها."
                   FontSize="14" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
